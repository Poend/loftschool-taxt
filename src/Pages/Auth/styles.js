export default {
  layoutBg: {
    background: `url(${process.env.PUBLIC_URL}/background.png)`,
    backgroundSize: 'cover',
    height: '100%',
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center'
  }
}